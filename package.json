{
  "name": "socket-paint-demo",
  "version": "0.1.0",
  "description": "A sample app for Learn to Code LA that lets users paint on a collaborative canvas in real time over a WebSocket connection. Built with NodeJS, Express and SocketIO.",
  "main": "app.js",
  "dependencies": {
    "express": "^4.10.2",
    "should": "^11.1.0",
    "socket.io": "^1.4.8"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "grunt": "latest",
    "grunt-mocha": "^1.0.2",
    "mocha": "^3.1.0",
    "really-need": "^1.9.2",
    "supertest": "^2.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "test": "mocha --reporter spec",
    "test_progression": "mocha --watch --bail --reporter spec"
  }
}
